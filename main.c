#include <stdio.h>



int check_is_digit(int ac , char *av[])
{
	int i = 0;
	int j;
	while(ac < av[i])
	{
		j = 0;
		while(av[i][j])
		{
			if()
		}
	}
}

int main(int ac , char *av[])
{
	if(ac != 6 && ac != 5)
		printf("not_valid");
	if((check_is_digit(ac , av) && check_num(ac ,av)) != 0)
		printf("invalid");
}
